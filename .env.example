# API Configuration
API_BASE_URL=http://localhost:8000/api

# Tenant Mode: 'multi' for subdomain-based tenants or 'single' for standalone deployment
# - 'multi': Supports multiple schools via subdomains (e.g., school1.localhost, school2.localhost)
# - 'single': Single school deployment on main domain (e.g., localhost, yourdomain.com)
TENANT_MODE=multi
